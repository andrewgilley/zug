const compatibility = @import("wasm/compatibility.zig");
const manifest = @import("wasm/manifest.zig");
const runtime = @import("wasm/runtime.zig");

test {
    _ = compatibility;
    _ = manifest;
    _ = runtime;
}
